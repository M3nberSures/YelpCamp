<% include partials/header %>

<div class="container">

    <header class="jumbotron my-4">
        <h1 class="display-4">Welcome to YelpCamp !</h1>
        <p class="lead">View our hand-picked campgrounds from all over the world.</p>
        <% if(typeof user != 'undefined'){ %>
            <a class="btn btn-primary btn-lg" href="#" data-toggle="modal" data-target="#modal-create-campground">add a new campground</a>
        <% } %>
    </header>

<!--    <div class="row text-center" style="display:flex; flex-wrap: wrap;">-->
<!--      <% campgrounds.forEach((campground) => { %>-->
<!--        <div class="col-md-3 col-sm-6">-->
<!--            <div class="card" style="height: 100%;">-->
<!--                <a href="/campgrounds/<%= campground._id %>"><img class="card-img-top img-thumbnail img-fluid" src="<%= campground.image %>" alt=""></a>-->
<!--                <div class="card-body">-->
<!--                    <h5 class="card-title"><%= campground.name %></h5>-->
<!--&lt;!&ndash;                    <h6 class="card-subtitle mb-2 text-muted">Card subtitle</h6>&ndash;&gt;-->
<!--&lt;!&ndash;                    <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>&ndash;&gt;-->
<!--                    <a href="/campgrounds/<%= campground._id %>" class="btn btn-primary">More Info</a>-->
<!--                </div>-->
<!--            </div>-->
<!--        </div>-->
<!--      <% }); %>-->
<!--    </div>-->

    <div class="row text-center">
        <% campgrounds.forEach((campground) => { %>
        <div class="col-lg-3 col-md-6 mb-4">
            <div class="card h-100">
                <img class="card-img-top" src="<%= campground.image %>" alt="">
                <div class="card-body">
                    <h4 class="card-title"><%= campground.name %></h4>
                </div>
                <div class="card-footer">
                    <a href="/campgrounds/<%= campground._id %>" class="btn btn-primary">More Info</a>
                </div>
            </div>
        </div>
        <% }); %>
    </div>

</div>
<% include partials/footer %>
